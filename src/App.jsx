import React from "react";
import { Button } from "./components/ui/button";

const App = () => {
  return (
    <div className="w-screen h-screen flex justify-center items-center">
      <Button>Hello (React + Shadcn + React Router Dom)</Button>
    </div>
  );
};

export default App;
